# frozen_string_literal: true

RSpec.describe Housekeeper do
  it 'has a version number' do
    expect(Housekeeper::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(false)
  end
end
